# ===========================================
# Attendkal Docker Environment Configuration
# ===========================================
# SECURITY WARNING: Generate strong passwords for production!
# Use: openssl rand -base64 32 for secure random passwords

# Server Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (REQUIRED - No defaults for security)
DB_USER=attendkal_user
DB_PASSWORD=mustafa63_Mustafa63_MUSTAFA63
DB_NAME=attendkal_db
DB_PORT=5432

# JWT Authentication (REQUIRED - No defaults for security)
# Generate with: openssl rand -base64 64
JWT_SECRET=mustafa63_Mustafa63_MUSTAFA63_63mustafa

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3001

# PgAdmin Configuration (Optional - use with --profile admin)
# Only enable for development/staging environments
PGADMIN_EMAIL=admin@attendkal.com
PGADMIN_PASSWORD=musmams63
PGADMIN_PORT=5050

# Security Notes:
# 1. Never commit .env files to version control
# 2. Use different passwords for each environment
# 3. Rotate secrets regularly
# 4. Use a secrets management service in production
